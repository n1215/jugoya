build:
    box:
      id: n1215/docker-test
      username: $USERNAME
      password: $PASSWORD
      tag: latest
    steps:
      - script:
        code: composer self-update
      - script:
        code: composer install
      - script:
        name: test & coverage
        code: composer coverage
      - script:
        name: move aritfacts
        code: mv ${WERCKER_SOURCE_DIR}/build ${WERCKER_OUTPUT_DIR}/

